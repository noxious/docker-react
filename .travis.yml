language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t povilasdev/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true povilasdev/docker-react yarn test
